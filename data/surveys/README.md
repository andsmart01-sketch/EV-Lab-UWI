# Survey Data

Google Form exports go here as CSV files.

NOTE: CSV files are excluded from Git tracking (.gitignore) to protect
respondent anonymity, even though no personal data is collected.
Keep local backups separately.

Files:
- consumer_fuel_survey_responses.csv (export from Google Forms weekly)
- juta_operator_survey_responses.csv (export after Week 3 deployment)
